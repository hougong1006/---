# SQ 现场手敲清单

共 11 行，按以下顺序完成。

## 1. 视觉端（2 行）

文件：`01_视觉端/yolov11.py`

第 29 行：

```python
MJPEG_PORT = 8765
```

第 370 行：

```python
_, _jpg_buf = cv2.imencode('.jpg', annotated_frame, [cv2.IMWRITE_JPEG_QUALITY, 70])
```

讲解：设置视频传输端口和图像压缩质量，建立稳定的视频输出。

## 2. 机械臂分拣端（8 行）

文件：`02_机械臂分拣端/yolov11_sortation.py`

从第 347 行开始连续输入：

```python
camera_location = self.pixel_to_camera_depth((cx, cy), dist)
PoseEndMat = np.matmul(self.EndToCamMat, self.xyz_euler_to_mat(camera_location, (0, 0, 0)))
EndPointMat = self.get_end_point_mat()
WorldPose = np.matmul(EndPointMat, PoseEndMat)
pose_T, pose_R = self.mat_to_xyz_euler(WorldPose)
pose_T[0] += self.x_offset
pose_T[1] += self.y_offset
pose_T[2] += self.z_offset
```

讲解：将识别到的像素坐标转换为机械臂基座坐标，再叠加现场标定补偿，得到最终抓取位置。

## 3. 可视化界面（1 行）

文件：`03_可视化界面/arm_monitor_window.py`

第 1963 行：

```python
self._cam_worker.start()
```

讲解：启动视频读取线程，将检测画面实时显示在可视化平台中。
