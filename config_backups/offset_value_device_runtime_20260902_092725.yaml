# Exact copy of the effective Jetson install-space configuration reported on
# 2026-09-02 before the next offset adjustment.

# Robot/world-frame grasp compensation, in metres.
# The former +0.020 m X correction in grasp() is now included here, so
# each Cartesian-axis correction is applied exactly once.
# Values preserve the calibration currently used by the running Jetson node.
# Default calibration: equivalent to the original running configuration.
# The +0.020 m X correction formerly in grasp() is now represented here.
x_offset: -0.007754
# Shift the landing point 10 mm left from the restored default calibration.
y_offset: -0.04541
z_offset: -0.0299
