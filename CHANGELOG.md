# 迭代记录

## 2026-08-26 - 第二轮比赛准备基线

- 从Jetson设备完整副本中提取实际运行代码。
- 保留标准件统计、缺陷件抓取的双类别流程。
- 记录15帧/10票检测确认参数。
- 记录夹爪闭合角度150及设备有效坐标补偿。
- 纳入机械臂控制、KDL运动学、可视化界面和一键启停脚本。
- 排除ROS构建产物、运行日志、缓存、旧备份和无关第三方工程。

后续每次优化在此追加日期、修改目标、测试条件和验证结果，并对应一个独立Git提交。

## 2026-08-26 - 六关节通信与运动自检

- 新增独立的`joint_self_test`工具，不启动ROS 2分拣节点。
- 依次检查六个舵机的通信响应、关节运动和角度读回结果。
- 加入重复启动保护及分拣进程互斥检查，防止多个程序同时控制I2C总线。
- 支持命令行一键启动和安全停止，正常结束、手动停止或异常退出后均尝试返回竖直姿态。
- 停止流程不使用强制终止，避免机械臂在归位途中失去程序控制。

## 2026-08-27 - 开发手册同步当前程序

- 将旧金桔三分类手册更新为航空丝堵标准件/缺陷件双分类业务。
- 校准15帧/10票、坐标补偿、夹爪角度、GPIO、MJPEG和日志统计等当前参数。
- 增加Windows实际运行界面、Jetson本地界面和六关节自检说明。
- 区分已实现功能、现有局限和推荐优化，移除已删除历史目录与旧启动流程。
- 明确外部运动学库、界面占位调参和多机械臂控制者等当前待验证风险。

## 2026-08-28 - 适配第二版模型类别标签

- 将模型类别更新为`biaozhunketi`（标准件）和`quexianketi`（缺陷件）。
- 标准件继续只进行投票确认和计数，不停止传送带、不进入抓取流程。
- 缺陷件继续执行投票确认、停带复检、机械臂抓取和分类投放。
- 同步更新分拣映射、可视化统计解析、类别中文显示和开发文档。
- 可视化日志解析暂时兼容旧标签，避免历史日志影响界面统计。

## 2026-08-28 - 可视化界面连接闪退修复补丁

- 修复Windows上位机连接Jetson后，后台SSH线程直接修改PyQt5控件可能引发界面闪退的问题。
- 将设备XYZ坐标补偿的读取结果改为通过Qt信号传回主线程，再安全更新界面控件。
- 为设备日志解析增加异常隔离，单条格式异常或乱码日志不再导致整个界面退出。
- 为使用`pythonw.exe`启动的界面增加全局异常和原生崩溃日志，异常信息写入界面目录下的`crash.log`，便于后续排查。
- 已完成相关Python文件语法检查，以及设备偏移量信号回传和界面更新回归测试。
- 未修改机械臂运动控制、坐标补偿参数、模型文件及分拣流程。

## 2026-08-29 - 工件离场误判过滤优化

- 针对工件从右向左运动、接近画面左侧时穿线孔被凹槽侧壁遮挡，标准件可能被误判为缺陷件的问题，新增横向有效检测区。
- 检测框必须完整位于`X=120～520`像素范围内，才允许标准件计数或缺陷件进入15帧/10票投票流程。
- 工件从左侧离开有效区时，立即清空尚未完成的缺陷投票，避免离场误判累积成停带和抓取指令。
- MJPEG画面增加有效区边界和`ACTIVE ROI`提示，便于现场根据相机位置和传送带速度调整参数。
- Windows可视化界面的默认设备IP更新为`10.182.135.172`，并自动迁移旧IP配置。
- 本次未修改模型、置信度、坐标补偿、机械臂姿态及夹爪参数。

## 2026-08-29 - 设备IP更正

- 经Jetson本机`hostname -I`与SSH连接确认，当前设备IP为`10.182.135.172`。
- 更正此前误记的`10.182.135.194`，并同步更新仓库文档、关节自检上传命令和Windows可视化界面。
- 可视化界面会将历史保存的旧IP自动迁移为当前IP；本次未修改检测、分拣或机械臂控制逻辑。

## 2026-08-29 - 系统停止联动传送带

- 优化`stop_sorting.sh`：在停止机械臂及ROS 2节点之前，先通过BCM6向传送带控制板输出50 ms停止脉冲。
- Windows可视化界面的停止按钮与命令行停止共用该脚本，二者现在都会同步停止传送带。
- 分拣节点正常退出时增加第二层停止脉冲保护，并在信号发送失败时输出明确警告。
- 本次未修改模型、投票参数、坐标补偿、机械臂动作姿态和夹爪参数。

## 2026-08-29 - 系统启动联动传送带

- 优化`start_sorting.sh`：GPIO配置完成后，先通过BCM13向传送带控制板输出50 ms启动脉冲，再启动各ROS 2节点。
- Windows可视化界面的一键启动与命令行启动共用该脚本，二者现在都会同步发送传送带启动指令。
- 启动脉冲发送失败时输出明确警告；文档增加节点初始化期间必须保持检测区域无工件的安全提示。
- 本次未修改模型、投票参数、坐标补偿、机械臂动作姿态和夹爪参数。
## 2026-09-01 - 可视化平台名称更新

- 将窗口标题和界面顶层标题统一更新为“壳体表面缺陷智能分拣系统可视化监控平台”。
- 本次仅调整界面显示名称，未修改设备连接、视频流、检测统计或启停控制逻辑。
## 2026-09-01 - 新增无视频流可视化界面

- 以当前维护版界面为基线新增`qt_arm_monitor_no_video`独立版本。
- 保留设备连接、一键启停、检测统计、趋势与分布图、设备运行状态和日志显示等功能。
- 移除MJPEG读取线程、8765端口访问、HTTP图像接收以及QImage/QPixmap视频处理，原视频区域改为静态“未启用视频”提示。
- 新版本与原有带视频界面相互独立，不覆盖当前维护版。
## 2026-09-01 - 修复无视频版快捷方式乱码

- 快捷方式名称改为通过Unicode字符码生成，避免Windows PowerShell 5按系统代码页读取UTF-8脚本时产生乱码和非法文件名。
- 程序路径和工作目录改为从`create_shortcut.ps1`所在目录自动获取，不再硬编码包含中文的绝对路径。
- 增加严格错误处理和创建结果验证，失败时不再错误提示“创建成功”。
## 2026-09-01 - 无视频版移除视频占位区域

- 完全删除“缺陷检测显示区域”“未启用视频”和无视频提示卡片，不再显示任何视频相关区域或文字。
- 顶部主体布局由三列调整为左右两列：检测统计/品质分析与设备状态/设备参数等宽铺满原视频区域。
- 底部检测趋势、运行日志和品质分布保持三列布局，所有控制、统计和日志逻辑不变。
## 2026-09-01 - 新增MJPEG视频流独立测试平台

- 新增`video_stream_tester`独立工具，只访问`http://设备IP:8765/`，不执行SSH控制、机械臂动作、传送带启停或检测统计。
- 支持设置设备IP和端口，独立连接、断开和显示MJPEG实时画面。
- 增加HTTP状态、Content-Type、分辨率、实时FPS、累计帧数、数据量、运行时间及最近错误等诊断信息。
- 提供不受Windows PowerShell中文代码页影响的桌面快捷方式创建脚本。
## 2026-09-01 - 视频测试工具改为完整监控界面

- 按现场使用要求，将原简化诊断窗口替换为与当前`机械臂监控`相同的完整可视化界面。
- 顶部新增“只连接视频”按钮，只访问`http://设备IP:8765/`，不建立SSH连接，也不启动机械臂、传送带或分拣系统。
- 按钮连接后切换为“断开视频”；连接地址、HTTP状态、首帧分辨率及错误信息写入原界面的运行日志区域。
- 保留原有“连接设备”、一键启停、检测统计、图表、设备状态和日志功能，与只连接视频功能相互独立。

## 2026-09-01 - 增加独立视频服务启停模式

- 新增`start_video_only.sh`，仅启动Orbbec相机、图像转换和YOLO/MJPEG三个视频节点，不启动机械臂驱动、逆运动学、分拣节点，也不发送传送带GPIO信号。
- 新增`stop_video_only.sh`，通过独立PID文件只停止由视频模式启动的进程，不影响完整分拣系统的进程管理。
- “只连接视频”按钮改为先通过SSH启动设备端独立视频服务，确认`8765`端口就绪后再读取MJPEG画面；无需预先运行分拣程序。
- 修复视频连接成功后按钮文字立即恢复为“只连接视频”的状态错误。
- 正式监控界面、无视频版、模型、投票参数、坐标补偿和机械臂动作流程均未修改。

## 2026-09-01 - 修复SSH连接后无法进入独立视频模式

- 修复视频测试界面先连接SSH时，普通MJPEG重连线程会阻止“只连接视频”按钮启动独立服务的问题。
- 点击“只连接视频”时会先停止尚未取得画面的普通重连线程，再启动设备端独立视频服务并连接`8765`端口。
- 本次仅修改视频测试版的连接状态切换，不影响正式监控界面和设备分拣程序。

## 2026-09-02 - 新增传送带独立启停脚本

- 新增`start_conveyor.sh`，只通过BCM13向传送带控制板输出一次50 ms启动脉冲，随后立即退出。
- 新增`stop_conveyor.sh`，只通过BCM6输出一次50 ms停止脉冲，随后立即退出。
- 两个脚本均不加载ROS 2环境，不启动或终止相机、YOLO、机械臂驱动、逆运动学及分拣节点。
- 输出完成或发生异常时均尝试恢复低电平并释放当前引脚，降低持续高电平和资源占用风险。
- 独立启停脚本每次执行时都会先通过`sudo`调用`setup_gpio.sh`，设备重启后无需再单独手动配置GPIO；配置失败时取消输出脉冲并返回错误。
- 为缩短现场命令，将独立启动脚本重命名为`1`，独立停止脚本重命名为`2`；执行方式简化为`~/1`和`~/2`。
- 新增短命令`3`作为六关节通信与运动自检的启动入口，内部继续调用原安全启动脚本，不改变关节测试动作和竖直归位姿态。
